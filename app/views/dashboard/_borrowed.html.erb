<%if @active_orders%>
  <h2 class ="text-light"> Les jeux que vous empruntez actuellement</h2>
  <div class='d-flex flex-wrap rented'>
  <%@active_orders.each do |order|%>
    <%order.renting.each_with_index do |rent, index|%>
      <%if rent%>
        <%@renting_copies.each do |copy|%>
          <div class='card col-3'>
            <img class="card-img-top" src="..." alt="Card image cap">
            <div class="card-body">
              <h5 class="card-title"><%=order.copies[index].game.title%></h5>
              <p class='card-text'><%=order.copies[index].game.description%></p>
              <a href="#" class="btn btn-primary">Jeux rendu?</a>
            </div>
          </div>
        <%end%> 
      <%end%>
    <%end%>
  <%end%>
  </div>
<%end%>
<h2 class ="text-light">Les jeux que vous avez emprunté</h2>
  <div class="container">
  <div class="row viewer">  
    <%if @past_orders[0] %>
      <div class='liste col-3'>
        <p>&nbsp;</p>
        <%= render 'list' %>
      </div>
      <div class='resume col-9'>
        <% order = [] %>
        <% if !order[0].nil? %>
          <%= render 'hist_order' %>
        <% end %>
      </div>
    <%end%>
  </div>
  </div>